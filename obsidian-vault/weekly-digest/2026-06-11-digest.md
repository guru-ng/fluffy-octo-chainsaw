# Weekly Digest — 2026-06-11

Covers repo activity for `guru-ng/fluffy-octo-chainsaw`, June 4–11, 2026.

(Note: GitHub network access wasn't available for this run, so this digest is based on local git history only — no PR/issue data.)

## Shipped this week

All activity landed on `main` on June 10, in three commits (with Claude Fable assisting):

- **feat: add projects/now/uses/contact pages, view transitions, SEO & comments** (`4d87a50`) — the big one. New `/projects`, `/now`, `/uses`, and `/contact` (Formspree) pages; view transitions via `ClientRouter`; JSON-LD structured data (WebSite, Person, BlogPosting) in `BaseHead`; reading time + table of contents derived from microCMS HTML; related-posts (shared tags) on articles; a Giscus comments component (placeholders at the time); Projects added to nav; fixed the `/contact` link on the about page; also fixed a `.gitignore` corruption issue (stray UTF-16 bytes).
- **fix: resolve astro check type errors in blog post layout** (`e962172`) — small type fixes in `BlogPost.astro` and `posts/[...slug].astro`.
- **feat: wire up real Formspree form ID and Giscus repo/category IDs** (`0310ed3`) — replaced the TODO placeholders from the previous commit with real Formspree and Giscus configuration on `Giscus.astro` and `contact.astro`.

Net effect: the site gained four new top-level pages, working comments and a contact form, richer SEO metadata, and view transitions — a solid jump in scope for the week, all following on from last week's microCMS migration work (sorting/pagination fix on `feb206e`, June 3).

## Content ideas status

All three ideas in `content-ideas.md` are still at **`idea`** status — none moved to `drafting` or `done` this week:

- *Lessons from migrating to Astro* (astro, meta, frontend)
- *My dev setup (2025 edition)* (tooling, productivity, setup)
- *Why I added a Notes section (and why it's not a blog)* (meta, writing, personal)

None are flagged as stale yet (this is the first digest, so no history to compare against), but all three have been sitting at `idea` since before this digest existed.

## Suggested next actions

1. **Draft "Lessons from migrating to Astro"** — this week's work (new pages, view transitions, SEO, comments, on top of the microCMS migration) is exactly the material this idea needs. The timing is good while it's fresh.
2. **Fill in the new `/now` and `/uses` pages with real content** — they shipped scaffolded this week; worth a pass to make sure they reflect your actual current focus and toolset before anyone visits them.
3. **Smoke-test the new Formspree form and Giscus comments** now that real IDs are wired up (`0310ed3`) — confirm submissions and comment threads actually work end-to-end on the deployed site.
